# Calibration files

This directory contains camera calibration CSV files retained for AM2 / Metashape workflow compatibility.

These files are input/reference data, not generated output. They are kept in the repository because existing configuration or workflow logic may refer to them.

Do not delete or replace these files unless the corresponding workflow references have been checked and updated.
