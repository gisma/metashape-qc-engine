# Legacy configurations

This directory contains older AM2-style, generated, or pre-migration configuration files.

These files are retained as reference material for tracing earlier workflow states and compatibility decisions. They are not active default configurations for `metashape-qc-engine`.

Use configurations from `config/experiments/` for current reproducibility and stability experiments.
