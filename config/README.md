# Configuration files

This directory contains YAML and CSV configuration files for `metashape-qc-engine`.

`base.yml` is retained as the top-level reference configuration.

Experiment-specific configurations and variant matrices are stored in `config/experiments/`.

Older AM2-style, generated, or pre-migration configurations are stored in `config/legacy/` and should not be treated as active defaults.
